#include <stdio.h>

int main(void) {
    int age = 21;
    float clock_speed_ghz = 3.22f;
    char symbol = 'W';

    printf("Age: %d\n", age);
    printf("Clock speed: %.2f GHz\n", clock_speed_ghz);
    printf("Symbol: %c\n", symbol);

    return 0;
}
